defmodule Miss.MapTest do
  use ExUnit.Case, async: true

  alias Miss.Map, as: Subject

  describe "from_struct/2" do
    test "when no options are given, behaves like Map.struct/1" do
    end

    test "when nested options is false, behaves like Map.struct/1" do
    end

    test "when nested options is true, converts all nested structs to map" do
    end
  end
end
